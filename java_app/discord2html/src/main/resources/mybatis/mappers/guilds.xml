<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pro.eng.yui.oss.d2h.db.mapper.GuildsMapper">

    <insert id="register" parameterType="Guilds">
        INSERT INTO guilds
            (guild_id, guild_name, joined_time, 
             anon_cycle, last_anon_changed, runs_on, on_run_message, on_run_url)
        VALUES
            (#{guild_id}, #{guild_name}, CURRENT_TIMESTAMP,
             #{anon_cycle}, CURRENT_TIMESTAMP, 12, 'on', 'deny')
    </insert>
    
    <update id="update" parameterType="Guilds">
        UPDATE guilds
        <set>
            <if test="guildName != null">
                guild_name = #{guild_name}
            </if>
            <if test="anonCycle != null">
                ,anon_cycle = #{anon_cycle}
            </if>
            <if test="runsOn != null">
                ,runs_on = #{runs_on, jdbcType=INTEGER}
            </if>
            <if test="onRunMessage != null">
                ,on_run_message = #{on_run_message}
            </if>
            <if test="onRunUrl != null">
                ,on_run_url = #{on_run_url}
            </if>
        </set>
        WHERE guild_id = #{guild_id}
    </update>
    
    <select id="selectOne" parameterType="Guilds">
        SELECT 
            guild_id, guild_name, joined_time, anon_cycle, runs_on, on_run_message, on_run_url
        FROM guilds
        WHERE guild_id = #{guild_id}
    </select>
    
    <select id="selectAll">
        SELECT
            guild_id, guild_name, joined_time, anon_cycle, runs_on, on_run_message, on_run_url
        FROM guilds
    </select>
    
    <update id="anonTimestamp" parameterType="Guilds">
        UPDATE guilds
        SET
            last_anon_changed = CURRENT_TIMESTAMP
        WHERE guild_id = #{guild_id}
    </update>

</mapper>